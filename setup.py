from setuptools import find_packages, setup

setup(
   name='src',
   packages=find_packages(),
   version='0.1.0',
   description="Analyze past borrowers' employment and credit history to predict new borrower's behavior in defaulting a loan.",
   author='Anthony Wong',
   license='',
)
